- name: install and run nginx
  hosts: [web,cart]
  tasks: 
  - name: install pacakages
    ansible.builtin.pacakge:
      name: nginx
      state: present
    ansible.builtin.service:
      name: nginx
      state: start
      
  